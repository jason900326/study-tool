name: Fix desktop uploader width

on:
  push:
    paths:
      - 'scripts/fix_desktop_uploader_width.py'
      - '.github/workflows/fix_desktop_uploader_width.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: python scripts/fix_desktop_uploader_width.py
      - name: Syntax check
        run: python -m py_compile streamlit_app.py
      - name: Commit updated app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add streamlit_app.py
          git diff --cached --quiet && exit 0
          git commit -m "Fix desktop material uploader width"
          git push
