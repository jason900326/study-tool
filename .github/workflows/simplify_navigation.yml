name: Simplify MedSlime navigation

on:
  push:
    paths:
      - 'scripts/simplify_navigation.py'
      - '.github/workflows/simplify_navigation.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply navigation patch
        run: python scripts/simplify_navigation.py
      - name: Syntax check
        run: python -m py_compile streamlit_app.py
      - name: Commit app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add streamlit_app.py
          git diff --cached --quiet && exit 0
          git commit -m "Simplify MedSlime navigation"
          git push
